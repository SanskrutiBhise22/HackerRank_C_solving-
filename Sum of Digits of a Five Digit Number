#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d", &n);
    int sum=0,rem;
    for(int i=0;i<5;i++){ 
        int rem=n%10;
        sum+=rem;
        n=n/10;
    }
    printf("%d",sum);
    return 0;
}
